namespace SportsLibrary.Model
{
    /// <summary>
    /// Universal interface for all event-specific additional data payloads.
    /// This allows the Timeline to store events with different types of payloads.
    /// </summary>
    public interface IEventPayload
    {
    }
}
